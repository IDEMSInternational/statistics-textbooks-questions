# Current plan — Descriptive-Statistics/Median/Median-of-7-integers/question.xml

A living description of what this question CURRENTLY is — not a
history of how it got there (see the .md metadata file for that),
and not necessarily the plan it started from, if that changed
during authoring. Written once a session is approved and merged;
empty until then.
